import React from 'react';
import MainChannel from './channel/main_channel_container';
const App = ({ children }) => {

  return (
    <div>
      { children }
    </div>
  );
};

export default App;
